<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Task Manager</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-blue-600 shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <a href="/" class="text-white text-xl font-bold">Task Manager</a>
                <div class="space-x-4">
                    <a href="/" class="text-blue-100 hover:text-white transition">Home</a>
                    <a href="/tasks" class="text-blue-100 hover:text-white transition">Tasks</a>
                    <a href="/tasks/new" class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded transition">New Task</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-4xl mx-auto px-4 py-16">
        <div class="text-center">
            <!-- Error Icon -->
            <div class="mb-8">
                <svg class="mx-auto h-24 w-24 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>

            <!-- Error Content -->
            <div class="bg-white rounded-lg shadow-lg p-8 max-w-2xl mx-auto">
                <h1 class="text-3xl font-bold text-gray-900 mb-4">Oops! Something went wrong</h1>
                <div class="mb-6">
                    <p class="text-lg text-gray-600 mb-4">
                        <%= message %>
                    </p>
                    <div class="bg-red-50 border border-red-200 rounded-lg p-4">
                        <p class="text-sm text-red-700">
                            Don't worry, this happens sometimes. You can try one of the options below to get back on track.
                        </p>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <button onclick="history.back()" 
                            class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold transition shadow-md">
                        Go Back
                    </button>
                    <a href="/tasks" 
                       class="bg-green-500 hover:bg-green-600 text-white px-6 py-3 rounded-lg font-semibold transition shadow-md">
                        View All Tasks
                    </a>
                    <a href="/" 
                       class="border border-gray-300 hover:border-gray-400 text-gray-700 px-6 py-3 rounded-lg font-semibold transition">
                        Go Home
                    </a>
                </div>

                <!-- Additional Help -->
                <div class="mt-8 pt-6 border-t border-gray-200">
                    <h3 class="text-lg font-semibold text-gray-900 mb-3">Need help?</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                        <div class="flex items-center text-gray-600">
                            <svg class="w-5 h-5 text-blue-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            Try refreshing the page
                        </div>
                        <div class="flex items-center text-gray-600">
                            <svg class="w-5 h-5 text-green-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                            </svg>
                            <a href="/tasks/new" class="hover:text-blue-600 transition">Create a new task</a>
                        </div>
                        <div class="flex items-center text-gray-600">
                            <svg class="w-5 h-5 text-purple-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                            </svg>
                            <a href="/tasks" class="hover:text-blue-600 transition">Browse your tasks</a>
                        </div>
                        <div class="flex items-center text-gray-600">
                            <svg class="w-5 h-5 text-yellow-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
                            </svg>
                            Check if the URL is correct
                        </div>
                    </div>
                </div>
            </div>

            <!-- Error Code Display (if needed) -->
            <div class="mt-8 text-center">
                <p class="text-sm text-gray-500">
                    Error occurred at <%= new Date().toLocaleString() %>
                </p>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-16">
        <div class="max-w-7xl mx-auto px-4 py-8 text-center">
            <p class="text-gray-300">
                © 2025 Task Manager. Built with Node.js, Express, and MongoDB.
            </p>
        </div>
    </footer>

    <script>
        // Add some interactivity
        document.addEventListener('DOMContentLoaded', function() {
            // Auto-refresh option
            const refreshBtn = document.createElement('button');
            refreshBtn.innerHTML = `
                <svg class="inline w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                </svg>
                Refresh Page
            `;
            refreshBtn.className = 'bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded text-sm font-medium transition mt-4';
            refreshBtn.onclick = function() { location.reload(); };
            
            const actionsDiv = document.querySelector('.flex.flex-col.sm\\:flex-row.gap-4.justify-center');
            if (actionsDiv) {
                actionsDiv.appendChild(refreshBtn);
            }
        });
    </script>
</body>
</html>